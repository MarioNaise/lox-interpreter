fun power(base, exp) {
  var result = 1;
  var negativeExp = false;

  if (exp < 0) {
    negativeExp = true;
    exp = -exp;
  }

  for (var i = 0; i < exp; i = i + 1) {
    result = result * base;
  }

  if (negativeExp) {
    return 1 / result;
  }

  return result;
}
